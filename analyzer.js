export async function analyzeWebsite(raw){let u;try{u=new URL(raw);if(!["http:","https:"].includes(u.protocol))throw 0}catch{throw Error("Enter a valid http/https URL.")}try{const r=await fetch(u.href,{mode:"cors"});if(!r.ok)throw Error("HTTP "+r.status);const doc=new DOMParser().parseFromString(await r.text(),"text/html"),imgs=[...doc.images],links=[...doc.querySelectorAll("a[href]")];let internal=0,external=0;for(const a of links){try{const x=new URL(a.href,u.href);x.hostname===u.hostname?internal++:external++}catch{}}const text=doc.body?.innerText||"";return{ok:true,url:u.href,title:doc.querySelector("title")?.textContent.trim()||"",description:doc.querySelector('meta[name="description"]')?.content||"",h1:doc.querySelectorAll("h1").length,images:imgs.length,noAlt:imgs.filter(x=>!x.alt.trim()).length,internal,external,canonical:doc.querySelector('link[rel="canonical"]')?.href||"",viewport:doc.querySelector('meta[name="viewport"]')?.content||"",lang:doc.documentElement.lang||"",words:text.trim()?text.trim().split(/\s+/).length:0}}catch(e){return{ok:false,url:u.href,message:"Browser blocked this site (CORS). A server-side analyzer is required for unrestricted URLs.",error:e.message}}}