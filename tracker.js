import{auth,db}from"./firebase-init.js";import{signInAnonymously}from"https://www.gstatic.com/firebasejs/12.2.1/firebase-auth.js";import{addDoc,collection,serverTimestamp}from"https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js";
function cid(){let k="fv_client_id",v=localStorage.getItem(k);if(!v){v=crypto.randomUUID?crypto.randomUUID():Date.now()+"-"+Math.random();localStorage.setItem(k,v)}return v}
export async function trackEvent(type,data={}){try{if(!auth.currentUser)await signInAnonymously(auth);await addDoc(collection(db,"analyticsEvents"),{type,clientId:cid(),platform:data.platform||navigator.platform,source:data.source||document.referrer||"direct",page:location.pathname,url:location.href,userAgent:navigator.userAgent.slice(0,300),language:navigator.language||"",timezone:Intl.DateTimeFormat().resolvedOptions().timeZone||"",...data,createdAt:serverTimestamp()});return true}catch(e){console.warn(e);return false}}
